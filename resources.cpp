#pragma once

#include <vector>
#include <string>

// restrict that size must be a square
std::vector<std::string> imageX = {
    R"(__  __)",
    R"(\ \/ /)",
    R"( \  / )",
    R"( /  \ )",
    R"(/_/\_\)"
};
